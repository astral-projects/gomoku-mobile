<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="gomoku.GomokuApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gomoku"
        tools:targetApi="31">
        <activity
            android:name="gomoku.ui.leaderboard.LeaderboardActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.variant.VariantActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.home.HomeActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.about.AboutActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.game.GameActivity"
            android:exported="false" />
        <activity
            android:name="gomoku.ui.login.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>